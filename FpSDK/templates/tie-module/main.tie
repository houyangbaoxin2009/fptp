type tie<logic>

// {{DisplayName}} —— tie 脚本插件（{{Id}}）
// tie 版本：Harbor-2026.1-preview.4（SDK：fptp_sdk.tie，零 tie-interp 依赖）
// 运行桥（FpSDK.TieKit.TieRunner）：
//   - 输入：环境变量 FPTP_TIE_INPUT = base64(宿主文本)
//   - 输出：stdout 一行 "FPTP_OK:<base64>" 或 "FPTP_ERR:<base64>"
// 解析 import 按工作目录（CWD），fptp_sdk.tie 需与本文件同目录（生成时已就位）。
// 插件逻辑只需实现 process(src)；main 调 fptp.bridge(process) 完成
//   输入读取 → process → FPTP_OK 应答。

import "fptp_sdk.tie" as fptp

// ==================== 插件逻辑 ====================

func process(src: string) -> string {
    // TODO: 在此实现插件逻辑（src 为宿主传入的 tie:data / 协议文本）
    return src
}

// ==================== 运行桥（勿删） ====================

func main() {
    fptp.bridge(process)
}