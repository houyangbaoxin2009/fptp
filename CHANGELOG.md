# 更新日志

## [1.4.4.1] — 2026-08-02

> 1.4.4.1 为热修复版，修复 1.4.4.0 引入的加载图片回归：GDI+ 流克隆陷阱导致加载部分图片报"GDI+ 中发生一般性错误"。

### 修复
- 修复加载图片回归：`LoadBitmapUnlocked` 从流创建位图后用 `Clone()` 取副本，.NET Framework 中流关闭后该副本访问像素仍会抛"GDI+ 中发生一般性错误"（换底色/裁剪/黑白/保存均受影响），现改为 `new Bitmap(tmp)` 生成真正的 GDI+ 独立副本，GUI 与 CLI 两处一并修复

## [1.4.4.0] — 2026-08-02

> 1.4.4 为深度稳定性修复版，修复主题记忆修复后继续排查发现的 8 类问题（悬空对象引用、双重解锁、CLI 口径、更新下载健壮性等）。

### 修复
- 修复加载失败后悬空图引用崩溃：小图（边长 <300）加载失败时 `pictureBox1.Image` 仍指向已释放位图，保存/打印/排版会碰已释放对象，现退出前清空引用
- 修复阶段导出无异常兜底：`ExportStage` 写盘失败（磁盘满/权限/GDI+ 错误）直接冒泡崩溃，现失败静默不影响主流程
- 修复换底色动画模式双重 UnlockBits：异常路径 catch 释放位图后 finally 又对已释放位图解锁，抛 ObjectDisposedException 掩盖原异常
- 修复智能裁剪零尺寸除零：目标宽高非法（手改配置为 0）时除零崩溃，现直接返回 null
- 修复设置加载预设空引用崩溃：`gen.presets` 缺失时 `Presets.Count` 先于初始化执行
- 修复设置格式判断大小写敏感：手改 JSON 顶层 `App/Gen/Lang` 大写会被误判旧格式导致数据丢失，现忽略大小写
- 修复语言/主题包 `con` 为 null 时空引用：手改 JSON 显式 `"con": null` 触发崩溃，现调用处全部空值防御
- 修复语言包提示文本花括号触发 FormatException：`msg.langFileInvalid`/`msg.themeFileInvalid` 中字面 `{id}`/`{name}` 被 `string.Format` 当占位符，现转义为 `{{id}}`/`{{name}}`
- 修复 CLI `--lang` 位置限制：置于子命令之前时被误判为旧版参数，现任意位置均生效
- 修复 CLI 参数解析吞 flag：缺值（如 `-i -o out.jpg`）会把下一个 flag 当路径，现识别 flag 即视为缺值
- 修复旧版 `-s` 非法值静默生效：`-s 5` 等非法尺寸静默当一寸处理，现报错退出
- 修复 CLI 批处理 `-c` 颜色口径：`none` 被拒、系统色名（如 Control）被静默当白色，现白名单与 bgcolor 命令一致
- 修复 CLI 批处理预设文件非法值未校验：预设文件手改越界值直接使用，现统一钳制
- 修复 CLI 批处理 `total==0` 口径矛盾：JSON 报 success 但退出码 1，现 success 与实际处理数一致
- 修复 CLI `ass settings` 输出格式不一致：PascalCase 且无 success 字段，现 camelCase 统一并带 success
- 修复更新检查主源成功但无 Release 时不回退：现主源返回空同样尝试备用平台
- 修复更新/地区检测请求无 ReadWriteTimeout：响应体读取卡死可挂起 5 分钟，现限时
- 修复下载安装包无超时/无校验/临时名冲突：WebClient 无超时、下载内容不校验、固定临时名多实例冲突，现流式下载带超时、校验 PE 头与大小、临时名 GUID 唯一
- 修复批处理自定义排版（预设 4）静默回退 5 寸：`case 4` 缺失，现使用自定义宽高
- 修复快捷键设置方向键误录：录制模式下方向键/翻页/Tab 等导航键被录为快捷键且拦截表格导航，现纯导航键不录制

## [1.4.3.0] — 2026-08-01

> 🎉 **成熟版本** — 1.4.3 为稳定性修复版，修复发布前排查发现的 22 个问题（资源泄漏、崩溃、CLI 口径等）。

### 修复
- 修复设置面板底色下拉索引错位：统一为 蓝 / 红 / 白 / 透明 顺序
- 修复换底色羽化方向颠倒：主体边缘不再被染色 / 抠空（`t = (tolerance + feather - diff) / feather`）
- 修复重复加载图片内存泄漏：加载前释放旧图
- 修复手改 `setting.json` 非法值导致启动崩溃：加载后统一校验回退
- 修复 `ass working` 误返回残留旧图：轮询前删除工作结果文件
- 修复输入框回车 / Esc 无效：AcceptButton / CancelButton 在控件创建后赋值
- 修复批处理后台线程跨线程读控件与异常崩溃：参数快照移 UI 线程、补充 `e.Error` 检查、输出格式按实际透明度
- 修复小图洪泛填充播种越界：播种范围限制在图片边界内
- 修复排版纸与定时器资源泄漏：切换操作释放排版纸、workingTimer 字段化并随窗体清理、失败删除请求文件、等待光标补 try/finally
- 修复预处理算法异常路径位图泄漏：4 个返回方法补 try/catch 兜底释放
- 修复主题包导出丢 alpha、导入不校验完整性：ToHex 保留半透明、导入前校验 8 键
- 修复设置导出丢失未确定的自定义尺寸、SaveFormat 空引用防御
- 修复快捷键动作表空引用防御
- 修复无扩展名误存 BMP、JSON 格式子串误判：无扩展名按 PNG 保存、JsonDocument 精确判断顶层 key
- 修复打赏弹窗关闭泄漏克隆位图
- 修复 CLI 批处理单张损坏图中止整批：逐张容错跳过
- 修复普通模式换底色采样缺陷：四角取最接近色，避免单角主体误采样
- 修复透明检测漏检：全量扫描 alpha，零星透明像素不再误存 JPG
- 修复语言包导出后无法自动加载：加载兼容 `lang.{id}.{name}.json` 命名
- 修复 CLI `-l 4` 静默失效与 `-c none` 口径不一：自定义排版生效、单张换底支持 none=透明
- 程序集版本号与 AppVersion 同步：csproj 增加 `<Version>`
- 补充 CHANGELOG 1.4.1.1 ~ 1.4.1.3 缺失条目

## [1.4.2.0] — 2026-08-01

> 1.4.2 围绕**主题自定义与目录整理**展开。

### 新增
- 内置多主题：跟随系统 / 浅色 / 深色 / 护眼绿 / 深空蓝，设置 → 界面主题 下拉直接切换，选择持久化到 `setting.json` 的 `app.themeId`
- 主题包系统：支持导入 / 导出主题包（`theme.{id}.{name}.json`，8 个调色板键可自定义），导入即应用；语言包 / 主题包独立导入导出，不随设置包导出
- 临时文件位置设置：设置 → 隐私 可选择处理中的图片存放位置（内存，不落盘隐私优先 / 硬盘 publish 目录）
- CLI 子命令 `ass working`：外部应用可获取 GUI 当前处理中的图片（内存临时文件），`fptp.exe ass working [-o out.jpg]`
- 批处理窗口新增"导入 bat 文件"按钮；导入的 bat 无批处理命令时给出错误提示并终止导入

### 变更
- 编译后目录整理：系统库 DLL → `lib\`（App.config probing 指向，运行时正常加载）、语言包 → `lang\`、图片 → `img\`、文档 → `doc\`、主题包 → `theme\`，安装脚本同步
- 语言包加载优先级调整：`exe\lang\` 目录语言包文件 > 内嵌资源，可直接编辑语言文件生效
- 主题包 / 语言包导入导出对话框默认目录改为 `theme\` / `lang\` 文件夹
- 导出设置包不再包含语言包与主题包（独立管理）

### 修复
- 修复主题调色板从未加载问题：`Theme.Init()` 在 v1.4.0.0 重构后未被调用，现于主窗体构造函数初始化

## [1.4.1.3] — 2026-08-01

### 新增
- 主题包功能：基于 `setting.json` 的 `theme` 段，可独立导入导出（`theme.{id}.{name}.json`），导出设置不再包含语言包与主题包

### 修复
- 修复主题调色板未加载问题（`Theme.Init()` 未被调用）

## [1.4.1.2] — 2026-08-01

### 修复
- 导入 bat 文件无批处理命令时改为错误提示并终止导入

## [1.4.1.1] — 2026-08-01

### 新增
- 批处理窗口新增"导入 bat 文件"按钮

## [1.4.1.0] — 2026-08-01

### 变更
- 移除"一键处理"按钮及其 F9 快捷键，功能由 智能裁剪 / 修改底色 / 排版 分步完成（快捷键设置界面同步移除对应项）
- 侧栏布局调整：排版栏上移至打印栏上方

### 修复
- 修复主界面按钮无响应问题：顶栏（设置/关于/撤回/重新开始）、导入、预处理、排版、完成区全部按钮及滑块、下拉框、复选框的事件绑定在 v1.4.0.0 重构时丢失，已全部补齐

## [1.4.0.0] — 2026-08-01

> 1.4 系列围绕**界面重构与更加自定义化**展开。

### 新增
- 主界面重构：顶栏导航（重新开始 / 撤回 / 设置 / 关于）+ 侧栏分组（导入 / 预处理 / 排版 / 完成）+ 状态栏
- 快捷键系统：`setting.json` 新增 `key` 段（基于 actions 字典），设置界面新增"快捷键"分组，可调整快捷键并恢复默认
  - 默认快捷键：Ctrl+R 重新开始、Ctrl+Z 撤回、Ctrl+, 设置、Ctrl+A 关于、Ctrl+O 加载、Ctrl+W 卸载、
    Ctrl+Shift+C 智能裁剪、Ctrl+G 变黑白、Ctrl+B 修改底色、Ctrl+L 排版、Ctrl+S 导出、Ctrl+P 打印、
    Ctrl+Shift+B 批处理、F9 一键处理
- 透明底色：换底色新增"透明"选项，输出透明背景 PNG（普通模式与动画模式均支持，边缘羽化自动去白边）
- 新增 `Theme.cs` 界面主题应用

### 变更
- 主界面由固定布局改为分区布局（顶栏 + 侧栏 + 主画布 + 状态栏）
- 设置界面新增"快捷键"分组（调整快捷键对话框入口），布局相应下移
- 含透明背景的图片保存时自动改用 PNG（JPEG/BMP/GIF 无 alpha 通道）

### 优化
- 换底色普通模式与动画模式均支持透明目标（背景像素 alpha 归零、边缘只衰减 alpha 保留原色）

## [1.3.6.0] — 2026-08-01

### 新增
- 文件夹批处理：选择输入/输出文件夹，后台线程逐张处理（裁剪 / 黑白 / 换底 / 排版可勾选），进度条实时反馈、可取消
- 一键处理：智能裁剪 → 换底色 → 排版 全流程一键完成
- 预设系统：保存 / 套用 / 删除 当前全部参数（尺寸、底色、容差、排版、输出格式与质量）
- 命令行子命令模式：`basic`（info/version）、`prep`（crop/grayscale/bgcolor/batch）、`ass`（save/checkres/settings）
- `prep batch` 支持 `--preset` 预设文件与直接参数，输出 JSON 结果

## [1.3.5.1] — 2026-08-01

### 新增
- 关于窗口"联系作者"新增 QQ 群入口（https://qm.qq.com/q/rM7Vy0YSiI）

## [1.3.5.0] — 2026-08-01

### 新增
- 新增"动画模式"：换底色使用连通域洪泛填充算法，保护动漫角色眼白、衣服等被轮廓包围的相似色区域不被误替换
  - 三层防护：四角种子（衣服不接触四角不会被播种）、梯度屏障（扩散遇描边线即停止）、面积兜底（背景占比异常高时自动降容差重试）
- 命令行 `prep bgcolor` 支持 `-a/--anime` 参数切换动画模式算法

### 优化
- 换底色算法新增边缘羽化：容差至容差+30 过渡带内按距离线性混合原色与新背景色，消除头发边缘抗锯齿产生的白边
- 普通模式换底色改用 LockBits 批量读写像素，性能大幅提升

## [1.3.3.1] — 2026-07-31

### 新增
- 新增应用图标：白底蓝红双色相框 + 人像剪影，扁平简洁风，小尺寸下清晰
- 图标接入 exe（ApplicationIcon）、安装包（SetupIconFile）、主窗体与任务栏

## [1.3.3.0] — 2026-07-31

### 修复
- 修复国内用户检测不到更新的问题
  - 原因：GitCode Releases API 数组为升序（旧版在前），`Updater.cs` 误取数组首个作为最新版本
  - 方案：遍历全部 Releases 取 tag 版本号最大者，兼容 GitCode（升序）与 GitHub（降序）两种顺序

## [1.3.2.0] — 2026-07-31

### 修复
- 修复安装到 Program Files 后非管理员启动报"保存设置失败：拒绝访问"
  - 原因：设置文件硬编码写到 exe 目录，普通用户对 Program Files 无写权限
  - 方案：设置文件路径优先 exe 目录，不可写时自动回退 `%APPDATA%\FPTP\setting.json`（启动时探测并缓存）
- 修复安装向导在 Inno Setup 6.7.3 下崩溃 "Runtime error: Could not call proc"
  - 原因：6.7.3 中访问 `TNewRadioButton.Checked` 属性必崩（即使用户正停留在该页）
  - 方案：改用 `TInputOptionWizardPage.Values[]` 索引读取选项，并在用户离开页面时缓存

### 优化
- 安装选项合并改为仅当值变化时才写盘，避免每次启动无谓写入
- 启动自动路径（设置迁移 / 安装选项合并）写盘失败静默，不弹错误框；用户主动操作仍提示

## [1.3.1.0] — 2026-07-31

### 新增
- 安装向导新增"文档安装"选项页：可选择安装 Markdown / PDF 文档或不安装（安装到 Program Files 后仍可用）
- 设置文件新增 high 隐藏段（docsFormat / installLang）：安装程序写入、应用读取，不入设置面板与导入导出
- 关于窗口文档按钮按安装的文档格式打开（.md / .pdf），选择"不安装"时禁用

### 变更
- 安装向导支持中英双语（[Languages] 段，跟随系统语言）
- 安装选项写入 `install-options.json`，应用启动时合并到设置隐藏段（high：docsFormat / installLang）
- 自动更新改为静默安装：/VERYSILENT /NORESTART /SUPPRESSMSGBOXES，不再弹安装向导

## [1.3.0.0] — 2026-07-31

### 新增
- 关于窗口重构分组：文档 / 支持与联系 / 操作
- 关于窗口新增"报告问题"按钮：按地区跳转 GitCode / GitHub Issues
- 关于窗口新增"联系作者"按钮：邮箱联系（3187909557@qq.com）
- 关于窗口显示当前更新源（按地区自动判断 GitCode / GitHub）

## [1.2.1.0] — 2026-07-31

### 新增
- 自动更新按用户地区选择源：中国用户从 GitCode 获取更新，国外用户从 GitHub 获取（IP 定位自动判断，无需配置）
- 新增 `RegionDetector.cs`：级联 IP 定位服务判断用户地区，进程内缓存只检测一次
- 所选更新源请求失败时自动回退另一平台

### 变更
- 更新源从单一 GitCode 扩展为 GitCode + GitHub 双平台

## [1.2.0.0] — 2026-07-31

### 新增
- 中英双语界面切换（设置 → 界面语言，支持 zh-CN / en-US，即时生效）
- 语言包独立导入/导出（设置 → 界面语言）：文件名 `lang.{id}.{name}.json`，内容为翻译表，导入后立即切换
- 命令行新增 `--lang` 参数指定启动语言
- 排版支持预设布局：5 寸、6 寸、A4、A5，以及自定义宽高（设置 → 排版布局）
- 自动更新（默认开启，可在设置中关闭）：启动时静默检查，关于窗口可手动检查，从 GitCode Releases 下载安装包
- 新增 `Updater.cs`（GitCode API 检查更新、下载、启动安装）、`CustomSizeBox.cs`（自定义尺寸输入）、`Lang.cs`（翻译资源加载）
- 输出格式扩展：保存支持 JPEG / PNG / BMP / TIFF / GIF 五种格式
- JPEG 质量可调（70-100，设置 → JPEG 质量，默认 100）
- 排版辅助线样式可选：虚线 / 实线 / 无（设置 → 辅助线样式）
- 新增"打印"按钮：PrintDocument 打印当前图片，自动缩放居中，可选手打印机

### 变更
- 排版按钮统一为一个"排版"按钮 + 布局下拉框（原 5 寸 / 6 寸两个按钮）
- 关于窗口与主界面全部文本迁移至语言包（lang.zh-CN.json / lang.en-US.json）
- 设置文件统一为 `setting.json`：`{app}{gen}{lang}` 三部分，旧格式（setting.json + gen_setting.json）自动迁移
- 语言加载优先级：设置文件语言包 → 内置嵌入资源 → 回退中文
- 仓库链接更新为 GitCode（https://gitcode.com/jiro2025/fptp）
- 完成区按钮重排：导出本地 / 打印 / 卸载图片 一行三列

## [1.1.1.1] — 2026-07-30

### 修复
- 修复捐赠弹窗在低分辨率屏幕上超出显示区域无法关闭的问题
  - 原因：收款码图片（1080×1383）直接作为窗口大小，部分屏幕容纳不下
  - 方案：缩放至屏幕工作区 80%，同时支持 Escape 键关闭弹窗

## [1.1.1.0] — 2026-07-30

### 新增
- 关于窗口增加功能按钮：查看更新日志、查看 README、查看贡献指南、查看 API 文档、GitHub、给作者买一杯咖啡、访问官网
- 添加 CHANGELOG.md 更新日志文件
- 完善 README.md（API 参考表格、详细使用说明）
- 添加 CONTRIBUTING.md 贡献指南（项目架构、编码规范、开发流程）

### 修复
- 修复 Designer 文件中 5 处事件处理程序缺失导致的编译错误（补回空实现桩）
- 修复 `-v` / `--version` 命令行参数无输出的问题
  - 原因：`WinExe` 子系统不关联父控制台
  - 方案：添加 `AttachConsole` / `FreeConsole` P/Invoke，CLI 模式主动附加父控制台

### 优化
- `Program.cs` CLI 模式增加退出码：成功返回 0，失败返回 1

## [1.1.0.0] — 2026-07-29

### 新增
- 命令行批处理模式：`-i input -o output -s size`
- `-v` / `--version` 参数显示版本号

### 变更
- 项目目标框架从 .NET 8 迁移到 .NET Framework 4.8
  - 最低支持 Windows 7 SP1（需安装 .NET Framework 4.8）
  - 项目格式改为 SDK 风格 `Microsoft.NET.Sdk`

### 修复
- 排版布局不再覆盖 `currentImage`，保存时读取 `pictureBox1.Image`
- 重复点击排版按钮时正确释放上一张排版图
- `Assalg.cs` 中 `GetImageDecoders()` 更正为 `GetImageEncoders()`
- `sourceImage.Clone()` 异常时不再污染源引用
- 多个 `Graphics`、`ImageAttributes`、`EncoderParameters` 添加 `using` 释放
- `BtnSave_Click` 添加 `finally` 块确保恢复光标
- 低分辨率图片无限制加载的问题（添加 300px 最低检查）

## [1.0.0.0] — 2026-07-28

### 初始版本
- 基本的 Windows Forms 证件照处理工具
- 支持智能裁剪（一寸 / 二寸 / 小二寸）
- 支持彩色转黑白（ColorMatrix 灰度算法）
- 支持背景颜色替换（色键 + 容差算法）
- 支持 5 寸（4×2）和 6 寸（5×2）排版输出
- 支持高质量 JPEG / PNG 导出
- 关于对话框
